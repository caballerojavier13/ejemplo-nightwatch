{
  "name": "e2e-nightwatch-angular",
  "description": "E2E testing TodoMVC AngularJS version using Nightwatch.js",
  "version": "1.0.0",
  "scripts": {
    "test": "nightwatch --config tests/nightwatch.json --env chrome --test tests/e2e/overview.spec.e2e.js  --reporter ./tests/html-reporter.js"
  },
  "dependencies": {
    "nightwatch": "^0.8.6"
  },
  "devDependencies": {
    "nightwatch-html-reporter": "^2.0.0"
  }
}
